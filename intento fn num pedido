
useEffect(()=> {
    const numPedido = async (cart, email)=>{
      const {response} = await axios.get (
        'https://strapiecommerce-production-23f3.up.railway.app/api/orders',
      {
        data: 2,
        users_permissions_users: 6
      }
      
    )
      setDatos(response.data)
      console.log(setDatos(response.data), "qq")
     
    }
    numPedido()
  }, [])

